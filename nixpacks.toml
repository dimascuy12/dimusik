[phases.setup]
nixPkgs = ["python311", "ffmpeg", "python311Packages.pip"]

[phases.install]
cmds = [
  "npm install",
  "python3 -m pip install -U yt-dlp --break-system-packages || python3 -m pip install -U yt-dlp",
  "python3 -m yt_dlp --version"
]

[start]
cmd = "node server.js"
